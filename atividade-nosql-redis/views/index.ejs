<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css" integrity="sha384-lZN37f5QGtY3VHgisS14W3ExzMWZxybE1SJSEsQp9S+oqd12jhcu+A56Ebc1zFSJ" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <title><%= titulo %></title>
  </head>
  <body>
    <div class="pagina">
      <h1><%= titulo %></h1>
      <ul class="menu">
        <li class="item">
          <a class="btn btn-info" href="/clientes">Clientes</a>
        </li>
        <li class="item">
          <a class="btn btn-info" href="/itens">Itens</a>
        </li>
      </ul>
      <%if (clientes.length > 0) { %>
        <form action="/compras/c" method="post">
          <div>
            <label for="cliente">Cliente</label>
          </div>
          <div>
            <select name="cliente" id="cliente" required autofocus>
              <option disabled selected value> -- Selecione um Cliente -- </option>
              <% for(var i = 0; i < clientes.length; i++) { %>
                <option value="<%= clientes[i] %>"><%= clientes[i] %></option>
              <% } %>
            </select>
          </div>
          <div>
            <button class="btn btn-sucesso" type="submit">Iniciar Compras</button>
          </div>
        </form>
      <% } %>
    </div>
  </body>
</html>
